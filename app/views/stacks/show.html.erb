<div class="links">

<h1><%= link_to @project.title, project_path(@project) %></h1>

<% if @top_card %>
  <p>selected card: 
  <%= @top_card.writing %>
  </p><br/>
<% end %>


<%#<div class="span8"> %>
<div>  
  <h3>versions:</h3>
  <ol class="listing">
    <%= render @cards %>
  </ol>
</div>

<div>
  
  <%= link_to 'add blank card to stack', edit_card_path(@blank_card) %>

</div class="span2 offset2">
  <% if @stack == @project.stacks.last %>
    <%= render 'form' %>
  <% end %>
</div>

