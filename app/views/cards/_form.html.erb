
<%= form_for(@card) do |f| %>
  <% if @card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% @card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    
      <%= f.label :writing, 'write & revise:' %> 
      <div>
        <%= f.text_area :writing, class: "card writing", autofocus: true %>
      </div>

      <div class="actions">
        <%= f.submit "keep version" %>
        <% if @current_stack.multi? %>
          <div style:"float:right">
            <%= link_to 'see versions (' + (@current_stack.cards.count - 1).to_s + ')' , stack_path(@current_stack)%>
          </div>
        <% end %>
      </div>




<% end %>


