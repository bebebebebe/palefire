
<%= form_for(@card, remote: true) do |f| %>
  <% if @card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% @card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    
      <%= f.label :writing, 'write & revise:' %> 
      <div>
        <%= f.text_area :writing, class: "card writing", autofocus: true %>
      </div>

      <div class="actions">
        <%= f.submit "keep version", class: "keep-btn" %>

        <span id="update_msg"></span>

        <% if @current_stack.multi? %>
          <div style:"float:right">
            <%= link_to 'see versions', stack_path(@current_stack) %>
            (<%= @current_stack.cards.count - 1 %>)
            
          </div>
        <% end %>

      </div>

<% end %>


