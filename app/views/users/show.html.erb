<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <div class="gravatar">
    <h1>
      <%= gravatar_for @user %>
      <%= @user.name %>
    </h1>
      </div>
    </section>
  </aside>

  <div class="span8">
    <% if @user.projects.any? %>
      <h3>projects</h3>
      <ol class="projects">
        <%= render @projects %>
      </ol>
      
    <% end %>
  </div>

</div>